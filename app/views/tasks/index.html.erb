<h1>Tasks list<h1>

<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task.id) %> - <%= link_to "Eliminar", task_path(task.id), method: :delete, data: { confirm: "Estás seguro?" } %></li>
<% end %>
